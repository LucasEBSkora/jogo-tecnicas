#ifndef SPELLMEMENTO_HPP
#define SPELLMEMENTO_HPP

#include "Memento.hpp"

namespace DIM
{
  class SpellMemento : public Memento {
  private:

  public:
    SpellMemento();
    ~SpellMemento();
  };
  
} // namespace DIM

#endif