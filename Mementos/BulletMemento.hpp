#ifndef BULLETMEMENTO_HPP
#define BULLETMEMENTO_HPP

#include "Memento.hpp"

namespace DIM
{
  class BulletMemento : public Memento {
  private:

  public:
    BulletMemento();
    ~BulletMemento();
  };
  
} // namespace DIM

#endif